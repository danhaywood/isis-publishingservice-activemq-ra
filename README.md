isis-publishingservice-activemq-rar
===================================

An implementation of an Apache Isis org.apache.isis.applib.services.publish.PublishingService that publishes to ActiveMQ using the RAR (JCA)